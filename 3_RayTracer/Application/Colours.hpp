const Colour Maroon = Colour (128, 0, 0);

const Colour DarkRed = Colour (139, 0, 0);

const Colour Brown = Colour (165, 42, 42);

const Colour Firebrick = Colour (178, 34, 34);

const Colour Crimson = Colour (220, 20, 60);

const Colour Red = Colour (255, 0, 0);

const Colour Tomato = Colour (255, 99, 71);

const Colour Coral = Colour (255, 127, 80);

const Colour IndianRed = Colour (205, 92, 92);

const Colour LightCoral = Colour (240, 128, 128);

const Colour DarkSalmon = Colour (233, 150, 122);

const Colour Salmon = Colour (250, 128, 114);

const Colour LightSalmon = Colour (255, 160, 122);

const Colour OrangeRed = Colour (255, 69, 0);

const Colour DarkOrange = Colour (255, 140, 0);

const Colour Orange = Colour (255, 165, 0);

const Colour Gold = Colour (255, 215, 0);

const Colour DarkGoldenRod = Colour (184, 134, 11);

const Colour GoldenRod = Colour (218, 165, 32);

const Colour PaleGoldenRod = Colour (238, 232, 170);

const Colour DarkKhaki = Colour (189, 183, 107);

const Colour Khaki = Colour (240, 230, 140);

const Colour Olive = Colour (128, 128, 0);

const Colour Yellow = Colour (255, 255, 0);

const Colour YellowGreen = Colour (154, 205, 50);

const Colour DarkOliveGreen = Colour (85, 107, 47);

const Colour OliveDrab = Colour (107, 142, 35);

const Colour LawnGreen = Colour (124, 252, 0);

const Colour ChartReuse = Colour (127, 255, 0);

const Colour GreenYellow = Colour (173, 255, 47);

const Colour DarkGreen = Colour (0, 100, 0);

const Colour Green = Colour (0, 128, 0);

const Colour ForestGreen = Colour (34, 139, 34);

const Colour Lime = Colour (0, 255, 0);

const Colour LimeGreen = Colour (50, 205, 50);

const Colour LightGreen = Colour (144, 238, 144);

const Colour PaleGreen = Colour (152, 251, 152);

const Colour DarkSeaGreen = Colour (143, 188, 143);

const Colour MediumSpringGreen = Colour (0, 250, 154);

const Colour SpringGreen = Colour (0, 255, 127);

const Colour SeaGreen = Colour (46, 139, 87);

const Colour MediumAquaMarine = Colour (102, 205, 170);

const Colour MediumSeaGreen = Colour (60, 179, 113);

const Colour LightSeaGreen = Colour (32, 178, 170);

const Colour DarkSlateGray = Colour (47, 79, 79);

const Colour Teal = Colour (0, 128, 128);

const Colour DarkCyan = Colour (0, 139, 139);

const Colour Aqua = Colour (0, 255, 255);

const Colour Cyan = Colour (0, 255, 255);

const Colour LightCyan = Colour (224, 255, 255);

const Colour DarkTurquoise = Colour (0, 206, 209);

const Colour Turquoise = Colour (64, 224, 208);

const Colour MediumTurquoise = Colour (72, 209, 204);

const Colour PaleTurquoise = Colour (175, 238, 238);

const Colour AquaMarine = Colour (127, 255, 212);

const Colour PowderBlue = Colour (176, 224, 230);

const Colour CadetBlue = Colour (95, 158, 160);

const Colour SteelBlue = Colour (70, 130, 180);

const Colour CornFlowerBlue = Colour (100, 149, 237);

const Colour DeepSkyBlue = Colour (0, 191, 255);

const Colour DodgerBlue = Colour (30, 144, 255);

const Colour LightBlue = Colour (173, 216, 230);

const Colour SkyBlue = Colour (135, 206, 235);

const Colour LightSkyBlue = Colour (135, 206, 250);

const Colour MidnightBlue = Colour (25, 25, 112);

const Colour Navy = Colour (0, 0, 128);

const Colour DarkBlue = Colour (0, 0, 139);

const Colour MediumBlue = Colour (0, 0, 205);

const Colour Blue = Colour (0, 0, 255);

const Colour RoyalBlue = Colour (65, 105, 225);

const Colour BlueViolet = Colour (138, 43, 226);

const Colour Indigo = Colour (75, 0, 130);

const Colour DarkSlateBlue = Colour (72, 61, 139);

const Colour SlateBlue = Colour (106, 90, 205);

const Colour MediumSlateBlue = Colour (123, 104, 238);

const Colour MediumPurple = Colour (147, 112, 219);

const Colour DarkMagenta = Colour (139, 0, 139);

const Colour DarkViolet = Colour (148, 0, 211);

const Colour DarkOrchid = Colour (153, 50, 204);

const Colour MediumOrchid = Colour (186, 85, 211);

const Colour Purple = Colour (128, 0, 128);

const Colour Thistle = Colour (216, 191, 216);

const Colour Plum = Colour (221, 160, 221);

const Colour Violet = Colour (238, 130, 238);

const Colour Fucsia = Colour (255, 0, 255);

const Colour Orchid = Colour (218, 112, 214);

const Colour MediumVioletRed = Colour (199, 21, 133);

const Colour PaleVioletRed = Colour (219, 112, 147);

const Colour DeepPink = Colour (255, 20, 147);

const Colour HotPink = Colour (255, 105, 180);

const Colour LightPink = Colour (255, 182, 193);

const Colour Pink = Colour (255, 192, 203);

const Colour AntiqueWhite = Colour (250, 235, 215);

const Colour Beige = Colour (245, 245, 220);

const Colour Bisque = Colour (255, 228, 196);

const Colour BlanchedAlmond = Colour (255, 235, 205);

const Colour Wheat = Colour (245, 222, 179);

const Colour CornSilk = Colour (255, 248, 220);

const Colour LemonChiffon = Colour (255, 250, 205);

const Colour LightGoldenRodYellow = Colour (250, 250, 210);

const Colour LightYellow = Colour (255, 255, 224);

const Colour SaddleBrown = Colour (139, 69, 19);

const Colour Sienna = Colour (160, 82, 45);

const Colour Chocolate = Colour (210, 105, 30);

const Colour Peru = Colour (205, 133, 63);

const Colour SandyBrown = Colour (244, 164, 96);

const Colour BurlyWood = Colour (222, 184, 135);

const Colour Tan = Colour (210, 180, 140);

const Colour RosyBrown = Colour (188, 143, 143);

const Colour Moccasin = Colour (255, 228, 181);

const Colour NavajoWhite = Colour (255, 222, 173);

const Colour PeachPuff = Colour (255, 218, 185);

const Colour MistyRose = Colour (255, 228, 225);

const Colour LavenderBlush = Colour (255, 240, 245);

const Colour Linen = Colour (250, 240, 230);

const Colour OldLace = Colour (253, 245, 230);

const Colour PapayaWhip = Colour (255, 239, 213);

const Colour SeaShell = Colour (255, 245, 238);

const Colour MintCream = Colour (245, 255, 250);

const Colour SlateGray = Colour (112, 128, 144);

const Colour LightSlateGray = Colour (119, 136, 153);

const Colour LightSteelBlue = Colour (176, 196, 222);

const Colour Lavender = Colour (230, 230, 250);

const Colour FloralWhite = Colour (255, 250, 240);

const Colour AliceBlue = Colour (240, 248, 255);

const Colour GhostWhite = Colour (248, 248, 255);

const Colour Honeydew = Colour (240, 255, 240);

const Colour Ivory = Colour (255, 255, 240);

const Colour Azure = Colour (240, 255, 255);

const Colour Snow = Colour (255, 250, 250);

const Colour Black = Colour (0, 0, 0);

const Colour DimGrey = Colour (105, 105, 105);

const Colour Grey = Colour (128, 128, 128);

const Colour DarkGrey = Colour (169, 169, 169);

const Colour Silver = Colour (192, 192, 192);

const Colour LightGrey = Colour (211, 211, 211);

const Colour Gainsboro = Colour (220, 220, 220);

const Colour WhiteSmoke = Colour (245, 245, 245);

const Colour White = Colour (255, 255, 255);